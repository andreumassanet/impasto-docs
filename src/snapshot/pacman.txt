# ╭──────────────────────────────────────────────────────────────────────────╮
# │                                                                          │
# │   P A C M A N                                                            │
# │   packages from the official repositories                                │
# │                                                                          │
# │   github.com/andreumassanet/impasto                                      │
# │                                                                          │
# ╰──────────────────────────────────────────────────────────────────────────╯

# · `./setup packages` installs whatever is missing with
# · `pacman -S --needed`. `#` starts a comment anywhere on a line, and a
# · commented-out line is skipped. The first two sections are required; each
# · later one is a feature that disables itself when its packages are missing.


# ── DESKTOP ─────────────────────────────────────────────────────────────────

hyprland                     # the compositor and hyprctl
quickshell                   # the shell: the bar, the island, the lock, notifications
awww                         # the wallpaper
imagemagick                  # the palette board
python                       # every backend in scripts/
git                          # setup's own clones, ./setup cursors, lazy.nvim
grim                         # the capture's photograph, and the lock screen's
dbus                         # dbus-monitor, which hears logind about to sleep
wl-clipboard                 # copying, and the clipboard's history
gtk3                         # gtk-launch, which the launcher opens applications with
libnotify                    # notify-send, which the timer rings with
fontconfig                   # fc-list, in fonts.py
pipewire                     # the volume, through Quickshell's Pipewire service
wireplumber                  # wpctl, which the media keys call
playerctl                    # the play, pause, next and previous keys
xdg-desktop-portal-hyprland  # screen sharing
xdg-desktop-portal-gtk       # every Open dialog
xdg-user-dirs                # the folders a capture lands in
xdg-utils                    # xdg-open, which yazi hands the rest to
qt6ct                        # the platform theme env.lua names
papirus-icon-theme           # what the impasto icon theme inherits
inter-font                   # the shell's type, and the greeter's
ttf-jetbrains-mono-nerd      # the terminal's type, and every glyph on the bar
adwaita-fonts                # the GTK windows' type
noto-fonts                   # the scripts the three above do not cover
noto-fonts-emoji             # emoji, in colour
noto-fonts-cjk               # Chinese, Japanese and Korean, and their punctuation


# ── TERMINAL ────────────────────────────────────────────────────────────────

kitty
zsh
starship
less                         # kitty's scrollback pager, which base does not bring


# ── QUICK CONTROLS ──────────────────────────────────────────────────────────

brightnessctl                # the laptop panel's brightness
ddcutil                      # an external monitor's, over DDC/CI
networkmanager               # the network tile, and nmcli
bluez                        # the bluetooth tile
bluez-utils                  # bluetoothctl, which switches it
power-profiles-daemon        # the power profile
upower                       # the battery
hyprsunset                   # the night light
hyprpicker                   # the colour under the pointer
pacman-contrib               # checkupdates, which counts the updates
hyprshutdown                 # SUPER + SHIFT + E exits through it, if installed


# ── CAPTURE ─────────────────────────────────────────────────────────────────

satty                        # annotating
tesseract                    # reading a region as text
tesseract-data-eng           # in English · one tesseract-data-* per language
wf-recorder                  # recording · wl-screenrec, in aur.txt, goes first
libpulse                     # pactl, which finds the output a recording hears
pipewire-pulse               # what pactl talks to


# ── TERMINAL PROGRAMS ───────────────────────────────────────────────────────

fzf                          # .zshrc reaches for it, and fzf-lua needs it
zoxide                       # .zshrc reaches for it if it is there
fnm                          # the same
fastfetch                    # the greeting, on `fa`
btop                         # themed by the palette push
cava                         # the same, and the bar's visualiser
yazi                         # the file manager in the terminal
imv                          # what yazi and the photo widget hand a picture to
mpv                          # what yazi hands a video or a song to
asciiquarium


# ── EDITOR ──────────────────────────────────────────────────────────────────

neovim
tree-sitter-cli              # compiles the parsers · base-devel has the compiler
ripgrep                      # the grep picker
fd                           # the file picker
lua-language-server          # each server starts only if its binary is there
pyright                      # or basedpyright, from the AUR, which lsp.lua prefers
ruff
bash-language-server
qt6-declarative              # qmlls6 · quickshell brings it anyway


# ── FILE MANAGER ────────────────────────────────────────────────────────────

thunar                       # SUPER + E
tumbler                      # thumbnails
ffmpegthumbnailer            # a video's thumbnail
gvfs                         # the trash, and mounting
xfconf                       # xfconf-query, which ./setup thunar writes with


# ── QT ──────────────────────────────────────────────────────────────────────

polkit-kde-agent             # the dialog that asks for a password
kconfig                      # kreadconfig6, for the KDE half of the palette


# ── CURSOR ──────────────────────────────────────────────────────────────────

hyprcursor                   # hyprcursor-util, which ./setup cursors compiles with


# ── HYPRLAND PLUGINS ────────────────────────────────────────────────────────

hyprpm                       # builds both plugins in ./setup plugins
base-devel                   # the compiler hyprpm builds with
cmake
meson
cpio


# ── LOGIN SCREEN ────────────────────────────────────────────────────────────

sddm                         # ./setup system
accountsservice              # the account's full name, which both screens show


# ── FACE UNLOCK ─────────────────────────────────────────────────────────────

v4l-utils                    # v4l2-ctl, which ./setup face and Settings find the infrared camera with


# ── DEVELOPMENT ─────────────────────────────────────────────────────────────

lua                          # luac, for ./setup check
inotify-tools                # ./setup sync --watch, which copies every save
